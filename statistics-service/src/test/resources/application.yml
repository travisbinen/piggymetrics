spring:
  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false
  data:
    mongodb:
      database: piggymetrics
      port: 0

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 6.0.9

eureka:
  client:
    enabled: false

rates:
  url: https://api.exchangeratesapi.io
