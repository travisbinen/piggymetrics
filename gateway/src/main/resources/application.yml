spring:
  application:
    name: gateway
  config:
    import: "configserver:http://config:8888"
  cloud:
    config:
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
